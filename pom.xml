<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.voxelhorizons</groupId>
    <artifactId>VoxelFurnitureShop</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>8</maven.compiler.release>
        <voxelcore.version>1.0-SNAPSHOT</voxelcore.version>
        <voxelfurniture.version>1.0-SNAPSHOT</voxelfurniture.version>
        <spigot.version>1.12.2-R0.1-SNAPSHOT</spigot.version>
        <junit.version>4.13.2</junit.version>
    </properties>

    <repositories>
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/groups/public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.voxelhorizons</groupId>
            <artifactId>voxelcore-common</artifactId>
            <version>${voxelcore.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.voxelhorizons</groupId>
            <artifactId>voxelcore-plugin</artifactId>
            <version>${voxelcore.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.voxelhorizons</groupId>
            <artifactId>VoxelFurniture</artifactId>
            <version>${voxelfurniture.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource><directory>src/main/resources</directory><filtering>true</filtering></resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration><release>${maven.compiler.release}</release></configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration><finalName>VoxelFurnitureShop-${project.version}-mc1.12-26.2</finalName></configuration>
            </plugin>
        </plugins>
    </build>
</project>

